#!/usr/bin/env sh

if [ -d ".git" ]; then
  if [ ! -f ".git/hooks/post-checkout" ]; then 
    exec ln -s ../../git-hooks/post-checkout .git/hooks/post-checkout
  else 
    echo "post-checkout exists - don't create"
  fi
else
  echo "not a git repo - skip"
fi

exit 0
