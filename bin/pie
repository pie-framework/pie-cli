#!/usr/bin/env node

let minimist = require('minimist');
let logFactory = require('../lib/log-factory');
let args = minimist(process.argv.slice(2));
logFactory.init(args.logLevel || args['log-level']);
require('../lib/pie').default(args);