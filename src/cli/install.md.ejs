# install 

Install the build dependencies. This happens automatically when you run the other commands (except for clean). So this is only really useful if you want to see how pie builds the output.

## Options

<%= loadFile('common-opts.md') %>

## Examples 

`pie install`

`pie install --dir some-dir 