* catalog-client update ui to optionally show config pane + markup editor
* catalog app - update to optionally bake in config pane ui into archive

* markdown element css loader - how to configure? 
* all in one - isnt really an all in one?
* look for reuse amongst app types
* fix up clean
* add tests for new apps: info/item

!! * why is webpack recompiling multiple times on `info`?
!! * serve runs catalog-demo 
